

# p, q p를 q로 나누면 나머지가 0이면 q는 p의 약수이다.
# if(p % q == 0)
# 1, 2, 3, 6
#  n과 k가 주어졌을때 k번째로 작은수를 구해라


# for(int i = 1; i <= n; ++i)
# { 
#       if(n % i == 0)
#           push.array(k);
#       if( array.size() < k)
#               return (-1);
#        return(array(k);)
# }
# 1. 약수를 구하기
# 2. 약수 중 k번째로 작은 수 찾기
# 3. 약수의 갯수가 k보다 작으면 -1 출력하기
#
# 알아야하는 문법  
# 주석처리하기
# For 문, if문 , 입력받기, 자료구조
